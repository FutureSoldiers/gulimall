spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.119.148:3306/gulimall_sms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: gulimall-coupon

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.sml
  global-config:
    db-config:
      id-type: auto
server:
  port: 7001
